Generic surface container — near-black panel, 1px muted border, violet glow on hover. Base for tracker rows, evidence blocks, related-app tiles.

```jsx
<Card><div>Reddit</div></Card>
<Card interactive={false}>Static content</Card>
```
