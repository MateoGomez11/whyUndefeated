Small uppercase mono tag. Use for category labels (Social, Content, Productivity) — never for threat level, which uses ThreatBadge's own semantic color system.

```jsx
<Badge>Social</Badge>
<Badge tone="brand">Featured</Badge>
```
