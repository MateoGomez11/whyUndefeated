Primary interactive action, mono-set label, violet on hover-glow.

```jsx
<Button variant="primary">Track this app</Button>
<Button variant="secondary" size="sm">View evidence</Button>
<Button variant="ghost">Skip</Button>
```

Use `primary` once per view for the single most important action. `secondary` for outlined alternatives. `ghost` for low-emphasis text actions.
