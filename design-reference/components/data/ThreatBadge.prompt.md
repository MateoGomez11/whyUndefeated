The site's core verdict signal. High = red/orange, Medium = amber, Low = green — a fixed, strictly semantic palette unrelated to the violet brand color.

```jsx
<ThreatBadge tier="high" />
<ThreatBadge tier="low" size="sm" />
```
