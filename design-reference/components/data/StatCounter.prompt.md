Odometer-style animated counter — counts up on mount/value change. Use for site-wide tallies (entries per threat tier) and single-app stats.

```jsx
<StatCounter value={3} label="High threat" />
<StatCounter value={340} suffix="M" label="Monthly active users" />
```
